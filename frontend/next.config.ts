import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "tied-beauty-java-semiconductor.trycloudflare.com",
    "*.trycloudflare.com"
  ]
};

export default nextConfig;
